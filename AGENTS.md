# Agent Instructions

Use [.agents/AGENTS.md](.agents/AGENTS.md) as the source of truth for this repo.

The short version: keep extraction behavior stable, run the full tests after parser changes, and do not let maintained source, test, or agent documentation files grow past the line-count policy.
